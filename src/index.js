// =============================================================================
// APPLICATION ENTRY POINT
// =============================================================================

"use strict";

module.exports = {
  register(/* { strapi } */) {
    // Register phase
  },

  bootstrap(/* { strapi } */) {
    // Bootstrap phase
  },
};
