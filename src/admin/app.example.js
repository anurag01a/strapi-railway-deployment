// =============================================================================
// ADMIN CUSTOMIZATION (rename to app.js to use)
// =============================================================================

const config = {
  locales: ["en"],
};

const bootstrap = (app) => {
  console.log(app);
};

export default {
  config,
  bootstrap,
};
